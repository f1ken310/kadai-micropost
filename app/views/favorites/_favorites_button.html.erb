  <% if current_user.favorite?(other_micropost) %>
  
    <%= form_with(model: current_user.favorite.find_by(micropost_id: other_micropost.id), local: true, method: :delete) do |f| %>
      <%= hidden_field_tag :micropost_id, other_micropost.id %>
      <%= f.submit 'Unfavorite', class: 'btn btn-danger btn-block btn-sm' %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
      <%= hidden_field_tag :micropost_id, other_micropost.id %>
      <%= f.submit 'Favorite', class: 'btn btn-primary btn-block　btn-sm' %>
    <% end %>
  <% end %>
